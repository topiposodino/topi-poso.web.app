/**
 * BubbleContainer.js
 * Styled Component for the genre bubble container
 */
import styled from "styled-components";

const BubbleContainer = styled.div`
    display: flex;
    flex-wrap: wrap;
`

export default BubbleContainer;