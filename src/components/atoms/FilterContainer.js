/**
 * FilterContainer.js
 * Styled Component container for the filter options on the home page
 */
import styled from "styled-components";

const FilterContainer = styled.div`
    display: flex;
    place-content: space-between;
    align-items: center;
    margin: 20px 0;
`;

export default FilterContainer;