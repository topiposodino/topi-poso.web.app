/**
 * InfoImage.js
 * Styled Component for Info page main image
 */
import styled from "styled-components";

const InfoImage = styled.img`
  width: 100%;
  margin: 0;
  padding: 0;
`

export default InfoImage